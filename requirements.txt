# Dependencies for the Vercel Python (ASGI) app in app.py.
# Installs the local markitdown package with common document formats.
# Heavy/optional extras (audio, youtube, azure) are excluded to keep the
# serverless bundle within size limits.
./packages/markitdown[pdf,docx,pptx,xlsx,outlook]
starlette
