# Dependencies for the Vercel Python (ASGI) app in app.py.
# Installs the local markitdown package with common document formats.
#
# NOTE: the Vercel serverless function bundle is capped at 250 MB unzipped.
# The `xlsx`/`xls` extras (pandas + numpy, ~50 MB) are intentionally left out
# so the MCP server and the other formats fit. Audio, youtube and azure
# extras are also excluded for the same reason.
./packages/markitdown[pdf,docx,pptx,outlook]
mcp
starlette
